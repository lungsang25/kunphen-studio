VITE_API_BASE_URL=http://localhost:8000
# Google OAuth Client ID from Google Cloud Console (leave empty to hide the Google button)
VITE_GOOGLE_CLIENT_ID=
# Show the dev-only login bypass button (never enable in production)
VITE_DEV_AUTH_ENABLED=false
